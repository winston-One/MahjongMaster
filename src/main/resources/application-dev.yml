spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/queshen?useUnicode=true&useSSL=false&serverTimezone=GMT&characterEncoding=UTF-8&rewriteBatchedStatements=true
    username: root
    password: 123456
  redis:
    host: localhost
    port: 6379
    password: 123456
    # 连接池配置
    lettuce:
      pool:
        max-active: 100
        max-idle: 100
        min-idle: 10
        time-between-eviction-runs: 100s
  #Servlet配置信息
  servlet:
    multipart:
      #最大传输 MultipartFile 大小 和 最大请求大小
      max-file-size: 10MB
      max-request-size: 10MB
#小程序信息
wechat:
# 小程序申请时候的appid，详细看readme文档
  appid: wx9***********dc
# 小程序申请时候的密钥，详细看readme文档
  secret: ba************e442

wx:
  pay:
    appId: wx9***********1dc #微信公众号或者小程序等的appid
    mchId: 16***********00 #微信支付商户号
    mchKey: Mj**********************741f #微信支付商户密钥
    #    subAppId: #服务商模式下的子商户公众账号ID
    #    subMchId: #服务商模式下的子商户号
    keyPath: classpath:apiclient_cert.p12
    # p12证书的位置，可以指定绝对路径，也可以指定类路径（以classpath:开头）